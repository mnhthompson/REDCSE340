

<h1><%- title %></h1>
<%- messages() %> 
<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>

  <form class="input-box" action="/inv/add-inventory" method="post">
    <label>Make<input type="text" value="<%- locals.inv_make %>" placeholder="Smart" name="inv_make" minlength="2" maxlength="50"><span class="fine-print">Required.</span></label>
    <label>Model<input type="text" value="<%- locals.inv_model %>" placeholder="Fortwo" name="inv_model" minlength="2" maxlength="50"><span class="fine-print">Required.</span></label>
    <label>Year<input type="number" value="<%- locals.inv_year %>" placeholder="2008" name="inv_year" min="1900" max="2090"><span class="fine-print">Required. 1900-2090.</span></label>
    <label>Description
        <textarea placeholder="Fancy and red." name="inv_description" minlength="2" maxlength="100" rows="5"><%- locals.inv_description %></textarea><span class="fine-print">Required. 100 characters max.</span>
    </label>
    <label>Image<input type="text" value="/images/vehicles/no-image.png" placeholder="" name="inv_image"><span class="fine-print">Required.</span></label>
    <label>Thumbnail<input type="text" value="/images/vehicles/no-image-tn.png" placeholder="" name="inv_thumbnail"><span class="fine-print">Required.</span></label>
    <label>Price<input type="number" value="<%- locals.inv_price %>" placeholder="15,000" name="inv_price"><span class="fine-print">Required. Numeric only, no comma.</span></label>
    <label>Miles<input type="number" value="<%- locals.inv_miles %>" placeholder="100,000" name="inv_miles"><span class="fine-print">Required. Numeric only, no comma.</span></label>
    <label>Color<input type="text" value="<%- locals.inv_color %>" placeholder="White" name="inv_color"><span class="fine-print">Required.</span></label>
    <label>Classification
        <%- classifications %><span class="fine-print">Required.</span>
    </label>
    <input type="submit" value="ADD">
  </form>